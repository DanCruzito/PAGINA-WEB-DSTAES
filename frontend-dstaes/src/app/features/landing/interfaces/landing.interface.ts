import { IconValue } from '../../../shared/ui/ui-icon/icons';

export interface LandingStartFeatureInterface {
    quantity?: number;
    head?: string;
    icon: IconValue;
    description: string;
}
